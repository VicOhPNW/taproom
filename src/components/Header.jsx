import React from 'react';

function Header(){
  let styledHeader = {
  };

  return (
    <div style={styledHeader}>
      <div className="jumbotron">
        <p>Taproom</p>
      </div>
    </div>
  );
}

export default Header;